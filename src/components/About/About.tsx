import React from 'react';

const About: React.FC = () => {
    return (
        <div>
            <h2>About the AutoComplete App</h2>
            <p>This is just an About page.</p>
        </div>
    );
};

export default About;
